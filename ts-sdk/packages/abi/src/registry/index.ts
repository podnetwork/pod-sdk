/**
 * Registry module for @podnetwork/abi
 *
 * Provides an instance-based registry for managing ABIs by contract address.
 */

export { createRegistry, type AbiRegistry, type RegistryOptions } from "./registry.js";
